language: node_js
sudo: required
os:
  - linux
node_js:
  - '8.10.0'

before_install:
  - export CHROME_BIN=chromium-browser
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start
script:
  - yarn install
  - yarn lint-js
  - yarn lint-scss
  - yarn unit
  - yarn build
